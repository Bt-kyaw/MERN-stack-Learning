<html lang="en">
    <%- include('./components/head') %>
<body>
    
    <%- include('./components/navbar') %>

    <% if(blogs.length){ %>
        <% blogs.forEach(blog => { %>

             <h1><%= blog.title %></h1>
             <p> <%= blog.intro  %></p>

         <% }) %>
    <% } else { %>
           <h1>There is no blogs.</h1>
        <% } %>
</body>
</html>